# First-milestone progress — 2026-09-08

The public Steam source restored usable quote evidence for all four cases without relaxing the four-hour freshness rule. The first managed collector cycle completed twelve successful read-only requests. It produced 100 conditional scenarios, of which 20 were positive under the declared assumptions. These alternatives share the same virtual starting capital.

## Recorded quote snapshot

| Item | Steam bid / quantity | Steam ask / quantity | DMarket eligible ask | DMarket unconstrained Target |
| --- | --- | --- | ---: | ---: |
| Fracture Case | $0.65 / 622 | $0.71 / 484 | $0.55 | $0.52 |
| Recoil Case | $0.39 / 10 | $0.40 / 1464 | $0.46 | $0.39 |
| Revolution Case | $0.29 / 2942 | $0.31 / 5260 | $0.60 | $0.45 |
| Kilowatt Case | $0.19 / 1045 | $0.20 / 15796 | $0.46 | $0.45 |

The source observations and IDs are preserved in [the full screen export](m1_source_screen_2026-09-08.json). Source timestamp meanings and limitations are documented in [the qualification report](STEAM_PUBLIC_SOURCE.md).

## Paper trial now open

`paper-fracture-kilowatt-20260908` models acquisition of 17 Fracture Cases for $9.35 from $10 virtual starting capital. The original prediction is `c6ccd4d3d7aa6c790d24331dbd34e2e621b609e5345d1395e1480d095ae210b5`. No marketplace transaction was sent.

At the captured prices, the original calculation projects $9.86 Steam Wallet proceeds, then 49 Kilowatt Cases costing $9.80. Conditional DMarket receipts are $19.60 and conditional net growth is $10.25, with $0.06 Wallet remaining. These are frozen predictions, not realized results. Future prices and quantities are not reserved, and additional operating costs remain the optimistic zero baseline.

The modeled outward eligibility time is 2026-09-15T17:46:29.754634+00:00. The route will need later observations for its Steam sale and return selection, followed by the separate modeled ten-day return visibility delay. Immediate transfer was assumed for this paper entry. A real route would use actual account/item timestamps. Expected completion time is still unknown; the 17-day floor is not a promised duration.

Open-route P&L remains null. No prediction error or learning outcome was created. Paper funds, holdings, and modeled delay are explicitly separated from confirmed transactions. An unfinished route remains visible; there is no interim valuation or automated stop-loss.

## Continuous evidence collection

A managed read-only collector was verified running at 2026-09-08T17:46:29.754634+00:00, process 12480, at a six-hour cadence. Its first cycle succeeded and its error log was empty. The public source uses no paid SteamApis requests. Existing local source/total request allowances remain in force; errors or exhaustion stop the collector and are recorded. This is a resource control, not an evaluation window.

From the v2 directory:

```powershell
.\scripts\collector.ps1 -Action Status
.\scripts\collector.ps1 -Action Stop
```

Process identity is checked against its recorded start time before stopping it. Restarting requires `-Action Start` and the chosen credential source (or environment variables). The process does not automatically restart after a system reboot. See `data/collector_process.json` for log paths.

## Milestone status

77 tests pass. Timely quote collection and extended local chart history now work for this sample. The first economic milestone remains open: this paper trial has not completed, no confirmed route has completed, and repeatable net growth after real costs and delays has not yet been established. The collector gathers evidence; it does not execute trades or automatically advance paper fills.
